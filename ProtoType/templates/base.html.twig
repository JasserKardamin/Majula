<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
        <meta name="description" content="" />
        <meta name="author" content="" />
        <title></title>
        
        <link rel="icon" type="image/x-icon" href="{{ asset('icons/favicon.ico') }}" />
        <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200&icon_names=search" />
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
        <link href="{{ asset('css/styles.css') }}" rel="stylesheet" />
    </head>
    <body >
        <!-- Navigation-->
        <nav class="navbar navbar-expand-lg navbar-light bg-light custom-header ">
            <div class="container px-4 px-lg-5">
               <a class="navbar-brand" href="{{ path('app_data') }}" >
               EspritVerse
                </a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation"><span class="navbar-toggler-icon"></span></button>
                <div class="collapse navbar-collapse" id="navbarSupportedContent">
                    <ul class="navbar-nav me-auto mb-2 mb-lg-0 ms-lg-4">

                        <li class="nav-item">
                            <a class="nav-link {% if app.request.attributes.get('_route') == 'app_data' %}active{% endif %}" 
                            aria-current="page" href="{{ path('app_data') }}">Home</a>
                        </li>

                        <li class="nav-item">
                            <a class="nav-link {% if app.request.attributes.get('_route') == 'show_data_myposts' %}active{% endif %}" 
                            href="{{ path('show_data_myposts') }}">Posts</a>
                        </li>

                       <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle {% if app.request.attributes.get('_route') in ['route_for_courses', 'route_for_tds', 'route_for_tests', 'route_for_exams'] %}active{% endif %}" 
                        id="navbarDropdown" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">Offers</a>
                        <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                            <li>
                                <a class="dropdown-item" href="{{ path('display_all_courses') }}">Courses</a>
                            </li>
                            <li>
                                <a class="dropdown-item" href="{{ path('display_all_tds') }}">Tds</a>
                            </li>
                            <li>
                                <a class="dropdown-item" href="{{ path('display_all_tests') }}">Tests</a>
                            </li>
                            <li>
                                <a class="dropdown-item" href="{{ path('display_all_exams') }}">Exams</a>
                            </li>
                        </ul>
                    </li>
                    </ul>
                  <form class="d-flex" action="{{ path('add_data') }}" method="POST">
                        <button class="btn btn-outline-dark" type="submit">Contribute</button>
                  </form>
                </div>
            </div>
        </nav>
        <!-- Header-->
        <header class="bg-dark py-5">
            <div class="container px-4 px-lg-5 my-5">
                <div class="text-center text-white">
                    <h1 class="display-4 fw-bolder">Discover our latest Resources </h1>
                    <p class="lead fw-normal text-white-50 mb-0">Enhance your skills with our diverse library of over 300+ materials.</p>
                    {% if app.request.attributes.get('_route') != 'show_data_myposts'  %}
                        <div class="search_div">
                            <form id="searchForm" action="{{ path('data_search') }}" method="POST">
                                {% if app.request.attributes.get('_route') == 'data_search' %}
                                    <input class="search" type="text" name="search_value" value="{{ searchvalue }}" placeholder="Search..."  onkeyup="fetchSuggestions(this.value)" >
                                {% else %}
                                    <input class="search" type="text" name="search_value"  placeholder="Search..."  onkeyup="fetchSuggestions(this.value)" >
                                {% endif %}
                                <ul class="search-options" id="search-options" style="display: none;">
                                </ul>
                                <button class="search-button" type="submit">
                                <span class="material-symbols-outlined" style="color: #343a40;">search</span>
                                </button>
                            </form>
                        </div>
                    {% endif %}
                </div>
            </div>
        </header>
        <!-- Section-->
        {% block data %}
        {% endblock %}
        <!-- Footer-->
        <footer class="py-5 bg-dark">
            <div class="container"><p class="m-0 text-center text-white">Copyright &copy; Your Website 2023</p></div>
        </footer>
        <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.8/dist/umd/popper.min.js" integrity="sha384-I7E8VVD/ismYTF4hNIPjVp/Zjvgyol6VFvRkX/vR+Vc4jQkC+hVqc2pM8ODewa9r" crossorigin="anonymous"></script>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.min.js" integrity="sha384-0pUGZvbkm6XF6gxjEnlmuGrJXVbNuzT9qBBavbLwCsOGabYfZo0T0to5eqruptLy" crossorigin="anonymous"></script>  
        <script src="{{ asset('js/scripts.js') }}"></script>
    </body>
</html>